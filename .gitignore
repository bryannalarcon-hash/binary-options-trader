# Secrets and keypairs
keys/
# Ignore ALL env files (at any depth) except the committed template. This
# covers .env, .env.local, .env.production, .env.*.backup, app/.env.*, etc.
.env*
!.env.example

# Local validator runtime artifacts
.test-validator.log
.test-validator.pid
test-ledger/

# All log files — never commit; some captured RPC URLs with a live API key.
*.log

# Build outputs (added pre-emptively for when Anchor / Next.js show up)
node_modules/
target/
.anchor/
.next/
*.tsbuildinfo
dist/
build/

# Process artifacts
.automation.log
.automation.pid
.app.log
.app.pid

# IDE
.vscode/
.idea/

# Tool / agent state — not meant for source control
.claude/
.claude-flow/
.bootstrap.log

# Test results (Playwright traces, videos, screenshots)
tests/test-results/
playwright-report/
test-results/
screenshots/

# Assignment brief + source material — not redistributable, not part of the repo
project_1771969779565.pdf
.meridian-zip-extracted/

# Internal working docs — not part of the submission (session handoffs,
# AI task breakdowns, internal research)
handoff/
docs/HANDOFF.md
docs/CODING_PRD.md
docs/PEAK6_RESEARCH.md
